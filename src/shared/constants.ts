import * as os from 'os';
import * as path from 'path';

export const CLAUDE_OUTPUT_STYLES_DIR = path.join(os.homedir(), '.claude', 'output-styles');
export const WEBSITE_URL = 'https://ccoutputstyles.vercel.app';